# Generated by Cargo
# will have compiled files and executables
/target/
/gen/schemas

# Binário sidecar do airpg-engine: ~40MB, reconstruído localmente (ver
# README.md "Rebuild do binário sidecar") — não versionado pra não inchar o
# histórico do repo antes de existir um pipeline de release de verdade.
/binaries/
